@model TenantListDto
@using Penna.Web.Extensions
@using System.Security.Claims

@{
    ViewData["Title"] = "Home Page";
}

<div class="d-none">
    @Html.DropDownListFor(m => m.Tenant.CountryId, Model.CountryList, "- Lütfen bir ülke seçiniz -", new { @class = "form-control" })
    @Html.DropDownListFor(m => m.Tenant.CityId, Model.CityList, "- Lütfen bir şehir seçiniz -", new { @class = "form-control" })
    @*@Html.DropDownListFor(m => m.Tenant.CreatedBy, Model.TownList, "- Lütfen bir ilçe seçiniz -", new { @class = "form-control" })*@
</div>

<div class="card">
    <div class="card-body">
        

    </div>
</div>
